window.mumbleWebConfig = {
  // Default values (can be changed by passing a query parameter of the same name)
  'defaults': {
    'username': ('c3lingo-web-' + Math.random().toString(36).substring(6)),
    'address': 'c3lingo.winkels.berlin', // window.location.hostname,
    'port': '64737'
  }
}
